// 8 minutos

// Slice:
// Use o método slice para extrair uma parte específica de um array de números.

const numbers = [10, 20, 30, 40, 50, 60, 70];

const slicedNumbers = numbers.slice(2, 5);
console.log(slicedNumbers);

// Esperado:
// [30, 40, 50]
