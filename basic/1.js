// 8 minutos

// Map
// Dado um array de números, peça aos membros do time para criar um novo array
// onde cada número seja duplicado.

const numbers = [1, 2, 3, 4, 5];
const doubledNumbers = numbers.map((number) => number * 2);
console.log(doubledNumbers);

// Esperado:
// [2, 4, 6, 8, 10]
